const ItemManager = (function() {
  let allItems = [];
  
  return {
    create(name = false, category = false, quantity = false) {
      let itemObj = {};
      let props  = [name, category, quantity];
      
      
    },
    
    
  };
})();